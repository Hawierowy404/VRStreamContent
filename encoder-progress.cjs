function progressReader(emit){let buffer='',frame={};return chunk=>{buffer+=chunk.toString();if(buffer.length>8192)buffer=buffer.slice(-8192);let end;while((end=buffer.indexOf('\n'))>=0){const line=buffer.slice(0,end).trim();buffer=buffer.slice(end+1);const [key,value]=line.split('=');if(['frame','fps','out_time_us'].includes(key)&&Number.isFinite(Number(value)))frame[key]=Number(value);if(key==='speed'){const speed=Number(value.replace(/x$/,''));if(Number.isFinite(speed))frame.speed=speed;}if(key==='progress'){emit({state:'telemetry',...frame});frame={};}}};}
module.exports={progressReader};
